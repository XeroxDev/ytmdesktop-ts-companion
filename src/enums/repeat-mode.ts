/**
 * Repeat mode.
 */
export enum RepeatMode {
    /**
     * Unknown repeat mode.
     */
    UNKNOWN = -1,
    /**
     * No repeat.
     */
    NONE = 0,
    /**
     * Repeat whole playlist.
     */
    ALL = 1,
    /**
     * Repeat current track.
     */
    ONE = 2,
}